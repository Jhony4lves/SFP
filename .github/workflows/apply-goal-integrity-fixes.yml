name: Apply Goal Integrity Fixes
on:
  push:
    branches: [qa/bug-zero-sweep-2026-09-02]
    paths:
      - tools/apply-goal-integrity-fixes.js
      - .github/workflows/apply-goal-integrity-fixes.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: qa/bug-zero-sweep-2026-09-02
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply goal integrity patch
        run: node tools/apply-goal-integrity-fixes.js
      - name: Commit goal integrity fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/assets/www/index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "fix(goals): make contributions explicit and transactionally coherent"
          git push origin HEAD:qa/bug-zero-sweep-2026-09-02
