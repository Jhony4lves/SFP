org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# Distribuições Android devem incrementar SFP_VERSION_CODE monotonamente.
# RC2 do ciclo 2.2: compara Antes × Depois no fim real do período simulado.
# Gate da RC3: importação mobile, PDF local e autoclassificação.
# Gate final após correções estáticas do fluxo de importação.
# Gate final da PR #74 após fast-path idempotente para reimportações duplicadas.
SFP_VERSION_CODE=11
SFP_VERSION_NAME=2.2.0-rc.2
